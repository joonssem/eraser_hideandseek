--- local
+++ live
@@ -159,11 +159,19 @@
   pointer-events:none;animation:frozenPulse 0.55s ease-in-out infinite;}
 #centerAlert.hidden{display:none;}
-#lobbyPracticeBox{position:fixed;inset:0;z-index:80;display:flex;align-items:center;justify-content:center;
+#lobbyPracticeBox,#serverPickBox{position:fixed;inset:0;z-index:80;display:flex;align-items:center;justify-content:center;
   background:rgba(0,0,0,.5);padding:16px;pointer-events:auto;}
-#lobbyPracticeBox.hidden{display:none;}
-#lobbyPracticeBox .lpCard{background:var(--cream);border:3px solid var(--ink);border-radius:16px;
+#lobbyPracticeBox.hidden,#serverPickBox.hidden{display:none;}
+#lobbyPracticeBox .lpCard,#serverPickBox .lpCard{background:var(--cream);border:3px solid var(--ink);border-radius:16px;
   padding:18px 20px;max-width:520px;width:100%;max-height:88vh;overflow-y:auto;text-align:center;
   box-shadow:5px 5px 0 rgba(0,0,0,.3);}
 /* 서버 선택 행: 이름 + 사용량 게이지 + 인원 */
+.srvRow{display:flex;align-items:center;gap:10px;border:3px solid var(--ink);border-radius:12px;
+  background:#fff;padding:10px 14px;margin:8px 0;cursor:pointer;box-shadow:2px 2px 0 rgba(0,0,0,.2);}
+.srvRow:not(.disabled):hover{background:var(--yellow);}
+.srvRow.disabled{opacity:.45;cursor:default;box-shadow:none;}
+.srvRow .srvName{font-size:15px;font-weight:800;min-width:78px;text-align:left;}
+.srvRow .srvBarWrap{flex:1;height:14px;border:2px solid var(--ink);border-radius:7px;background:#eee;overflow:hidden;}
+.srvRow .srvBar{height:100%;width:0%;background:var(--blue);transition:width .4s;}
+.srvRow .srvCount{font-size:13px;font-weight:800;min-width:96px;text-align:right;}
 @keyframes frozenPulse{0%,100%{transform:translate(-50%,-50%) scale(1);}50%{transform:translate(-50%,-50%) scale(1.08);}}
 
@@ -334,7 +342,6 @@
 #wipe.run{display:block;animation:wipeAnim .6s linear forwards;}
 @keyframes wipeAnim{from{left:-30%;}to{left:110%;}}
-#setupNotice{position:fixed;bottom:10px;left:50%;transform:translateX(-50%);z-index:120;
-  background:rgba(230,69,69,.95);color:#fff;font-size:13px;padding:10px 16px;border-radius:10px;
-  max-width:min(92vw,560px);text-align:center;line-height:1.55;font-weight:700;box-shadow:0 4px 14px rgba(0,0,0,.35);}
+#setupNotice{position:fixed;bottom:10px;left:50%;transform:translateX(-50%);z-index:45;
+  background:rgba(230,69,69,.95);color:#fff;font-size:13px;padding:8px 16px;border-radius:10px;max-width:92vw;text-align:center;}
 #mirrorHint{position:fixed;bottom:14px;left:50%;transform:translateX(-50%);z-index:40;
   color:#fff;font-size:14px;text-shadow:1px 1px 0 #000;pointer-events:none;text-align:center;}
@@ -346,55 +353,4 @@
 </script>
 <script src="https://cdn.jsdelivr.net/npm/qrcode-generator@1.4.4/qrcode.js"></script>
-
-<!-- ════════════════════════════════════════════════════════════════
-     ★★★ 선생님 설정 구역 — 이 파일에서 수정할 곳은 여기뿐입니다! ★★★
-
-     [Firebase 준비 방법 — 5분 소요, 무료]
-     1) https://console.firebase.google.com 접속 → "프로젝트 추가"
-        (구글 계정만 있으면 됩니다. 애널리틱스는 꺼도 됩니다.)
-     2) 왼쪽 메뉴 [빌드] > [Realtime Database] > "데이터베이스 만들기"
-        - 위치는 아무 곳이나(가까운 asia-southeast1 추천)
-        - "테스트 모드"로 시작
-     3) [규칙] 탭에서 아래처럼 되어 있는지 확인(테스트 모드면 기본):
-        { "rules": { ".read": true, ".write": true } }
-        ※ 테스트 모드는 30일 뒤 만료됩니다. 만료되면 규칙 탭에서
-          만료 날짜만 늘려주거나 위 규칙으로 다시 저장하면 됩니다.
-     4) [프로젝트 개요] 옆 ⚙️ > [프로젝트 설정] > 아래 "내 앱"에서
-        웹 앱( </> 아이콘 ) 추가 → firebaseConfig 코드가 나옵니다.
-     5) 그 firebaseConfig의 중괄호 { } 안 내용을 복사해서,
-        아래 중괄호 안의 내용과 통째로 바꿔치기 해주세요.
-        ※ databaseURL 줄이 없다면 2)번(Realtime Database 만들기)을
-          먼저 한 뒤 다시 복사하면 databaseURL이 포함되어 나옵니다.
-
-     [배포 방법]
-     이 index.html 파일 하나를 https://app.netlify.com/drop 에
-     드래그&드롭하면 끝! 나오는 주소를 학생들에게 알려주세요.
-     ════════════════════════════════════════════════════════════════ -->
-<script>
-/* ▼▼▼ 여기서부터 ▼▼▼  (아래 값들을 선생님의 Firebase 설정으로 바꾸세요) */
-const firebaseConfig = {
-  apiKey: "여기에_붙여넣기",
-  authDomain: "여기에_붙여넣기",
-  databaseURL: "여기에_붙여넣기",
-  projectId: "여기에_붙여넣기",
-  storageBucket: "여기에_붙여넣기",
-  messagingSenderId: "여기에_붙여넣기",
-  appId: "여기에_붙여넣기"
-};
-
-/* 학생이 함부로 방을 만들지 못하게 막는 "선생님 코드"입니다.
-   원하는 말로 자유롭게 바꿔서 쓰세요. */
-const TEACHER_CODE = "하루담이";
-/* ▲▲▲ 여기까지 ▲▲▲  (이 아래로는 수정하지 마세요) */
-
-window.FIREBASE_CONFIG = firebaseConfig;
-window.TEACHER_CODE = TEACHER_CODE;
-</script>
-<script>
-/* 위 설정 구역에 문법 오류(붙여넣기 실수 등)가 있어도
-   게임이 완전히 멈추지 않고 안내 메시지를 띄울 수 있게 하는 안전장치입니다. */
-if (typeof window.FIREBASE_CONFIG === "undefined") window.FIREBASE_CONFIG = null;
-if (typeof window.TEACHER_CODE === "undefined" || !window.TEACHER_CODE) window.TEACHER_CODE = "선생님";
-</script>
 </head>
 <body>
@@ -616,4 +572,15 @@
 </div>
 
+<!-- 방 개설: Firebase 서버 선택 (선생님 코드 통과 후) -->
+<div id="serverPickBox" class="hidden">
+  <div class="lpCard">
+    <h3 style="margin-bottom:6px;">🖥 서버 선택</h3>
+    <div class="note" style="margin-bottom:8px;">게임방을 만들 서버를 골라주세요. 서버당 동시접속 100명이에요.</div>
+    <div id="serverListBox"></div>
+    <div class="row" style="margin-top:12px;justify-content:center;">
+      <button id="btnServerPickClose" class="secondary">닫기</button>
+    </div>
+  </div>
+</div>
 
 <div id="paintPanel" class="hidden">
@@ -666,28 +633,68 @@
 
 /* ============================================================
-   Firebase 설정은 이 파일 맨 위 <head>의 "선생님 설정 구역"에서 합니다.
-   (여기 아래 코드는 수정하지 마세요.)
+   Firebase 설정 — 아래 중괄호 안에 Firebase 콘솔의 설정값을 붙여넣으세요.
+   1) https://console.firebase.google.com 에서 프로젝트 생성
+   2) 빌드 > Realtime Database > 데이터베이스 만들기 (테스트 모드)
+   3) 규칙: { "rules": { ".read": true, ".write": true } } 로 시작
+   4) 프로젝트 설정 > 웹 앱 추가 > firebaseConfig 객체를 그대로 복사
+   ※ databaseURL 항목이 반드시 포함되어야 합니다.
    ============================================================ */
-/* 설정 상태를 점검해, 문제가 있으면 한국어 안내문을 돌려줍니다. 정상이면 null. */
-function fbConfigProblem(){
-  const c=window.FIREBASE_CONFIG;
-  if(!c||typeof c!=="object")
-    return "⚠️ 설정 구역에 오류가 있어요. 파일 맨 위 \"선생님 설정 구역\"에서 ① 기존 예시를 지운 자리에 붙여넣었는지 ② import로 시작하는 줄은 빼고 firebaseConfig = { ... } 부분만 붙여넣었는지 확인해 주세요.";
-  const need=["apiKey","authDomain","databaseURL","projectId","appId"];
-  const isPlaceholder=v=>!v||typeof v!=="string"||v.includes("여기에")||v.trim()==="";
-  if(need.every(k=>isPlaceholder(c[k])))
-    return "👋 아직 Firebase 설정이 입력되지 않았어요. 파일 맨 위 \"선생님 설정 구역\"의 안내(1~5번)를 따라 무료 Firebase 프로젝트를 만들고 설정값을 붙여넣은 뒤, 파일을 다시 배포해 주세요.";
-  if(isPlaceholder(c.databaseURL))
-    return "⚠️ 설정에 databaseURL이 빠져 있어요. Firebase 콘솔에서 [빌드 > Realtime Database > 데이터베이스 만들기]를 먼저 한 다음, 프로젝트 설정에서 firebaseConfig를 다시 복사해 붙여넣어 주세요.";
-  const miss=need.find(k=>isPlaceholder(c[k]));
-  if(miss) return `⚠️ 설정에 ${miss} 값이 비어 있어요. Firebase 콘솔의 firebaseConfig를 통째로 다시 복사해 붙여넣어 주세요.`;
-  if(!/^https:\/\//.test(c.databaseURL))
-    return "⚠️ databaseURL 값이 올바른 주소(https://...) 형태가 아니에요. 따옴표 안 내용을 다시 확인해 주세요.";
-  return null;
-}
-/* 게임 로직은 서버 목록 구조를 그대로 사용합니다(선생님 서버 1개). */
+const FIREBASE_CONFIG = {
+  apiKey: "AIzaSyCNYJ170nZmfshIiWt22sdx3PREgnXnzb8",
+  authDomain: "find-eraser-s1.firebaseapp.com",
+  databaseURL: "https://find-eraser-s1-default-rtdb.asia-southeast1.firebasedatabase.app",
+  projectId: "find-eraser-s1",
+  storageBucket: "find-eraser-s1.firebasestorage.app",
+  messagingSenderId: "462799497360",
+  appId: "1:462799497360:web:9c9b14a7f7ee214b96d015"
+};
+
+/* ============================================================
+   멀티 서버 — 한 서버(Firebase 프로젝트)당 동시접속 약 100명.
+   새 서버를 열려면 위와 같은 config 객체를 만들어 아래 null 자리에 넣으면
+   자동으로 '준비중'이 풀리고 선택 가능해집니다.
+   ★ 방 코드는 자유로운 4자리(전 서버 통틀어 유일하게 발급). 학생이 코드/QR로 입장하면
+     활성 서버들을 병렬 조회해 그 방이 있는 서버로 자동 연결됩니다(findRoomServer).
+   ============================================================ */
 const FIREBASE_SERVERS=[
-  {idx:1,name:"우리 반 서버",config:fbConfigProblem()?null:window.FIREBASE_CONFIG},
+  {idx:1,name:"서버 1",config:FIREBASE_CONFIG},
+  {idx:2,name:"서버 2",config:{
+    apiKey: "AIzaSyAS7FtoD1GzijGna4mDC9fHNRZmHqz-K1s",
+    authDomain: "find-eraser-s2.firebaseapp.com",
+    databaseURL: "https://find-eraser-s2-default-rtdb.asia-southeast1.firebasedatabase.app",
+    projectId: "find-eraser-s2",
+    storageBucket: "find-eraser-s2.firebasestorage.app",
+    messagingSenderId: "937513580449",
+    appId: "1:937513580449:web:ac67f01840802cf3767de0"
+  }},
+  {idx:3,name:"서버 3",config:{
+    apiKey: "AIzaSyCpsDeizsmh--DWKh-FzcH8pzkKjQ4bXrw",
+    authDomain: "find-eraser-s3.firebaseapp.com",
+    databaseURL: "https://find-eraser-s3-default-rtdb.asia-southeast1.firebasedatabase.app",
+    projectId: "find-eraser-s3",
+    storageBucket: "find-eraser-s3.firebasestorage.app",
+    messagingSenderId: "253323414301",
+    appId: "1:253323414301:web:1e23a2789d70a19d9deff4"
+  }},
+  {idx:4,name:"서버 4",config:{
+    apiKey: "AIzaSyCtZ_SIagVznbYq_3TS6Ajmcy3XNJW04HY",
+    authDomain: "find-eraser-s4.firebaseapp.com",
+    databaseURL: "https://find-eraser-s4-default-rtdb.asia-southeast1.firebasedatabase.app",
+    projectId: "find-eraser-s4",
+    storageBucket: "find-eraser-s4.firebasestorage.app",
+    messagingSenderId: "829957567208",
+    appId: "1:829957567208:web:a40e5e29a524a514c5cf5d"
+  }},
+  {idx:5,name:"서버 5",config:{
+    apiKey: "AIzaSyAngCjz7ZoV6aQ88MfKFFin6LIQDSJZQh4",
+    authDomain: "find-eraser-s5.firebaseapp.com",
+    databaseURL: "https://find-eraser-s5-default-rtdb.asia-southeast1.firebasedatabase.app",
+    projectId: "find-eraser-s5",
+    storageBucket: "find-eraser-s5.firebasestorage.app",
+    messagingSenderId: "120934496430",
+    appId: "1:120934496430:web:06279d9627d48f6d5a153f"
+  }},
 ];
+const SERVER_CAP=100;   // 서버당 동시접속 한도(표시용)
 
 /* ================= 유틸 ================= */
@@ -3491,5 +3498,5 @@
     if(this.db&&this.serverIdx===serverIdx)return true;
     if(!srv||!srv.config||!srv.config.databaseURL){
-      $("setupNotice").textContent=fbConfigProblem()||"⚠️ Firebase 설정을 확인해 주세요.";
+      $("setupNotice").textContent=`⚠️ ${srv?srv.name:"해당 서버"}는 아직 준비 중이에요. 다른 서버를 선택해 주세요.`;
       $("setupNotice").classList.remove("hidden");
       return false;
@@ -3620,11 +3627,5 @@
   },
   async joinRoom(code,nick){
-    // Firebase 설정이 아직 안 된 파일이면, 엉뚱한 "방 없음" 대신 설정 안내를 띄웁니다.
-    const problem=fbConfigProblem();
-    if(problem){
-      $("setupNotice").textContent=problem;
-      $("setupNotice").classList.remove("hidden");
-      return "prep";
-    }
+    // 코드가 어느 서버의 방인지 전 서버에서 찾아 연결(코드에 서버 번호를 넣지 않아도 됨)
     const sIdx=await this.findRoomServer(code);
     if(sIdx==null)return "noroom";
@@ -4602,21 +4603,51 @@
 $("btnCreate").onclick=()=>{
   Audio2.resume();
-  // 배포 전 설정이 안 됐으면, 코드 입력 전에 바로 안내를 띄워줍니다.
-  const problem=fbConfigProblem();
-  if(problem){
-    $("setupNotice").textContent=problem;
-    $("setupNotice").classList.remove("hidden");
-    $("titleNote").textContent="Firebase 설정이 필요해요. 화면의 안내를 확인해 주세요.";
-    return;
-  }
   const teacherCode=prompt("선생님 방 만들기 코드를 입력해 주세요.");
   if(teacherCode===null)return;
-  if(teacherCode.trim()!==String(window.TEACHER_CODE)){$("titleNote").textContent="선생님 코드가 올바르지 않아요.";return;}
+  if(teacherCode.trim()!=="하루담이"){$("titleNote").textContent="선생님 코드가 올바르지 않아요.";return;}
   const n=getNick()||"선생님"; $("nickInput").value=n;
-  createRoomOnServer(n,1);   // 코드 통과 → 바로 방 개설
+  openServerPicker(n);   // 코드 통과 → 서버 선택 → 방 개설
 };
+/* ── 서버 인원 조회: 해당 서버 /presence에서 최근 90초 내 하트비트만 집계 ── */
+async function countServerOnline(srv){
+  try{
+    const appM=await import("https://www.gstatic.com/firebasejs/9.23.0/firebase-app.js");
+    const dbM=await import("https://www.gstatic.com/firebasejs/9.23.0/firebase-database.js");
+    let app; try{ app=appM.getApp("srv"+srv.idx); }catch(_e){ app=appM.initializeApp(srv.config,"srv"+srv.idx); }
+    const db=dbM.getDatabase(app);
+    const snap=await dbM.get(dbM.ref(db,"presence"));
+    if(!snap.exists())return 0;
+    const now=Date.now(); let n=0;
+    Object.values(snap.val()).forEach(p=>{ if(p&&p.online&&typeof p.lastSeen==="number"&&now-p.lastSeen<90000)n++; });
+    return n;
+  }catch(e){ console.warn(srv.name+" 인원 조회 실패",e); return null; }
+}
+function openServerPicker(nick){
+  const list=$("serverListBox"); list.innerHTML="";
+  FIREBASE_SERVERS.forEach(srv=>{
+    const row=document.createElement("div");
+    row.className="srvRow"+(srv.config?"":" disabled");
+    row.innerHTML=`<div class="srvName">🖥 ${srv.name}</div>
+      <div class="srvBarWrap"><div class="srvBar" id="srvBar${srv.idx}"></div></div>
+      <div class="srvCount" id="srvCount${srv.idx}">${srv.config?"인원 확인 중…":"준비중"}</div>`;
+    if(srv.config) row.onclick=()=>createRoomOnServer(nick,srv.idx);
+    list.appendChild(row);
+  });
+  $("serverPickBox").classList.remove("hidden");
+  // 살아있는 서버들의 접속 인원 비동기 표시
+  FIREBASE_SERVERS.filter(s=>s.config).forEach(async srv=>{
+    const n=await countServerOnline(srv);
+    const c=$("srvCount"+srv.idx), b=$("srvBar"+srv.idx);
+    if(!c)return;
+    if(n==null){ c.textContent="조회 실패"; return; }
+    c.textContent=`${n} / ${SERVER_CAP}명`+(n>=SERVER_CAP?" 🈵":"");
+    if(b){ b.style.width=Math.min(100,n/SERVER_CAP*100)+"%";
+      b.style.background=n>=SERVER_CAP*0.9?"var(--red)":(n>=SERVER_CAP*0.6?"#e07f3e":"var(--blue)"); }
+  });
+}
 async function createRoomOnServer(nick,serverIdx){
+  $("serverPickBox").classList.add("hidden");
   const btn=$("btnCreate");
-  btn.disabled=true; $("titleNote").textContent="방을 만드는 중...";
+  btn.disabled=true; $("titleNote").textContent=`${FIREBASE_SERVERS[serverIdx-1].name}에 방을 만드는 중...`;
   try{
     const ok=await Net.createRoom(nick,serverIdx);
@@ -4634,4 +4665,6 @@
   }
 }
+$("btnServerPickClose").onclick=()=>$("serverPickBox").classList.add("hidden");
+$("serverPickBox").onclick=e=>{ if(e.target===$("serverPickBox"))$("serverPickBox").classList.add("hidden"); };
 $("btnJoin").onclick=async()=>{
   Audio2.resume();
@@ -4644,5 +4677,5 @@
     const res=await Net.joinRoom(code,n);
     if(res==="noroom"){ $("titleNote").textContent="그 코드의 방을 찾을 수 없어요."; return; }
-    if(res==="prep"){ $("titleNote").textContent="서버 설정에 문제가 있어요. 화면 아래 안내를 확인해 주세요."; return; }
+    if(res==="prep"){ $("titleNote").textContent="그 방은 아직 준비 중인 서버에 있어요. 코드를 다시 확인해 주세요."; return; }
     if(res==="full"){ $("titleNote").textContent="방이 가득 찼어요! (최대 50명)"; return; }
     if(res!=="ok"){ $("titleNote").textContent="Firebase 연결에 실패했어요. 화면 아래 오류 안내를 확인해 주세요."; return; }
